name: Automation script

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  automation_script_for_ci_cd:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        run: |
          ls -l 
          cat note.txt